# WRITE_ADDON.md — 보조 생활 포스트 작성 규칙

YMYL Level C. 쿠팡 파트너스 전용 영역.

## 필수 섹션 순서
1. `intro` — 이 용품의 쓰임새 + 보험과의 관계 (보험 권유 X)
2. `toc`
3. `h2` 왜 필요한가 (보험과의 보완 관계)
4. `h2` 선택 기준 (인증·AS·가격대)
5. `h2` 관련 보험 카테고리
6. `h2` 쿠팡 파트너스 링크 → `productSlot` 섹션
7. `h2` FAQ
8. `medDisclaimer`

## 절대 금지
- 보험상품 가입 권유
- "이 용품 사면 보험 필요 없다" 같은 오해 유도

## 쿠팡 링크
- 본문에 URL 직접 작성 금지
- `productSlot` 섹션의 `productKey`로 어드민 매칭
- `data/coupang-seed.json` 에 제안 항목 추가
